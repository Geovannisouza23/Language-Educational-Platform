apiVersion: apps/v1
kind: Deployment
metadata:
  name: course-service
  labels:
    app: course-service
spec:
  replicas: 3
  selector:
    matchLabels:
      app: course-service
  template:
    metadata:
      labels:
        app: course-service
    spec:
      containers:
      - name: course-service
        image: ghcr.io/language-platform/course-service:latest
        ports:
        - containerPort: 8080
        env:
        - name: COURSE_SERVICE_ENVIRONMENT
          value: "production"
        - name: COURSE_SERVICE_DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: course-secrets
              key: database-url
        - name: COURSE_SERVICE_REDIS_URL
          valueFrom:
            secretKeyRef:
              name: course-secrets
              key: redis-url
        - name: COURSE_SERVICE_JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: course-secrets
              key: jwt-secret
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "250m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: course-service
spec:
  selector:
    app: course-service
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
  type: ClusterIP
